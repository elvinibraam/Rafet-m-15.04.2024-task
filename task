//        1.Proqram yazın istifadeçidən söz veya cumle alsın və bu texti tersden yazdırsın.
//        Note:String text =“abc”;
//        abc.length() =3

        Scanner scanner = new Scanner(System.in);
        System.out.print("Söz və ya cümlə daxil edin: ");
        String text = scanner.nextLine();

        String reversedText = "";
        for (int i = text.length() - 1; i >= 0; i--) {
            reversedText += text.charAt(i);

        }

        System.out.println("Tərsə çevrilmiş text: " + reversedText);
